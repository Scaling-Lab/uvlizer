{% include 'product-content.zipifypages', contentprt: 'header' %}{% render 'shogun-products', content: product %}
{{product.metafields.shogun.above}}

{% if product.title == 'ROOM spray' or product.title == 'Natural Premium Bamboo Set' %}
  <div class="discount-banner">
    <span><span>24H sale get <strong>the bundle of 3 & save 35% OFF</strong></span> <span class="button">Code automatically APPLIED</span></span>
  </div>
  <style>
    .gallery__image--zoom:before {
      content: ' ';
      right: 0;
      top: 0;
      position: absolute;
      height: 100px;
      width: 100px;
      background-image: url('https://cdn.shopify.com/s/files/1/0519/0238/2231/files/Group_2273.png?v=1712147964');
      background-size: cover;
    }

    .button--primary__filled:hover {
      background-color: #d53600;
      color: #fff;
    }
  </style>
{% endif %}

<div class="row">
<div class="col-12 breadcrumbs--default">
{% render 'breadcrumbs' %}
</div>
</div>
{% section 'product-page__product' %}
{% section 'api__upsell--inline' %}
{% section 'product-page__recommended' %}

{% comment %} PRODUCT FORM CAN BE FOUND INSIDE 'product-page__product.liquid' {% endcomment %}					
<!-- RYVIU APP :: WIDGET -->
<!--<div class="lt-block-reviews"> 
	<ryviu-widget handle="{{product.handle}}" product_id="{{product.id}}" title_product="{{product.title}}" image_product="{{ product.featured_image.src | img_url: '180x' }}"></ryviu-widget>
</div>-->
<!-- / RYVIU APP -->
{% render 'judgeme_widgets', widget_type: 'judgeme_review_widget', concierge_install: false, product: product %}

{{product.metafields.shogun.below}}

<script type="text/javascript">
  var _learnq = _learnq || [];

  var item = {
    Name: {{ product.title|json }},
    ProductID: {{ product.id|json }},
    Categories: {{ product.collections|map:'title'|json }},
    ImageURL: "https:{{ product.featured_image.src|img_url:'grande' }}",
    URL: "{{ shop.secure_url }}{{ product.url }}",
    Brand: {{ product.vendor|json }},
    Price: {{ product.price|money|json }},
    CompareAtPrice: {{ product.compare_at_price_max|money|json }}
  };

  _learnq.push(['track', 'Viewed Product', item]);
  _learnq.push(['trackViewedItem', {
    Title: item.Name,
    ItemId: item.ProductID,
    Categories: item.Categories,
    ImageUrl: item.ImageURL,
    Url: item.URL,
    Metadata: {
      Brand: item.Brand,
      Price: item.Price,
      CompareAtPrice: item.CompareAtPrice
    }
  }]);
</script>

{% include 'product-content.zipifypages', contentprt: 'footer' %}
<!--Start Stamped.io Auto Installation--><div id="stamped-main-widget" class="stamped-main-widget" data-widget-style="standard" data-product-id="{{ product.id }}" data-name="{{ product.title | escape }}" data-url="{{ shop.url }}{{ product.url }}" data-image-url="{{ product.featured_image | product_img_url: "large" |replace: '?', '%3F' | replace: '&','%26'}}" data-description="{{ product.description | escape }}" data-product-sku="{{product.handle}}" data-product-type="{{product.type}}" data-widget-language="{{shop.locale}}">{{ product.metafields.stamped.reviews }}</div><!--End Stamped.io Auto Installation-->

<div id="myModal" class="modal">
  <div class="modal-content">
<span class="close">&times;</span>
  <img src="https://cdn.shopify.com/s/files/1/0519/0238/2231/files/splitnew.png?v=1660025184" width="100%">
</div>
</div>

<script>
// Get the modal
var modal = document.getElementById("myModal");

// Get the button that opens the modal
var btn = document.getElementById("mybtn");

// Get the <span> element that closes the modal
var span = document.getElementsByClassName("close")[0];

// When the user clicks the button, open the modal 
btn.onclick = function() {
  modal.style.display = "block";
}

// When the user clicks on <span> (x), close the modal
span.onclick = function() {
  modal.style.display = "none";
}

// When the user clicks anywhere outside of the modal, close it
window.onclick = function(event) {
  if (event.target == modal) {
    modal.style.display = "none";
  }
}
</script>

<style>



/* The Modal (background) */
.modal {
  display: none; /* Hidden by default */
  position: fixed; /* Stay in place */
  z-index: 1; /* Sit on top */
  padding-top: 100px; /* Location of the box */
  left: 0;
  top: 0;
  width: 100%; /* Full width */
  height: 100%; /* Full height */
  overflow: auto; /* Enable scroll if needed */
  background-color: rgb(0,0,0); /* Fallback color */
  background-color: rgba(0,0,0,0.4); /* Black w/ opacity */
}

/* Modal Content */
.modal-content {
  background-color: #fefefe;
  margin: auto;
  padding: 20px;
  border: 1px solid #888;
  width: 60%;
}

/* The Close Button */
.close {
  color: #aaaaaa;
  float: right;
  font-size: 28px;
  font-weight: bold;
}

.close:hover,
.close:focus {
  color: #000;
  text-decoration: none;
  cursor: pointer;
}
  div#myModal {
    z-index: 9999;
}

.discount-banner {
  width: 100%;
  height: 64px;
  display: flex;
  align-items: center;
  justify-content: center;
  background: #091B21;
  color: #fff;
  font-size: 18px;
}
.discount-banner > span {
  display: flex;
  align-items: center;
}
.discount-banner span {
  color: #fff;
  text-transform: uppercase;
}
.discount-banner .button {
  color: #081B21;
  background: #fff;
  padding: 4px 15px;
  border-radius: 16px;
  margin-left: 8px;
}

@media (max-width: 992px) {
  .discount-banner {
    height: 56px;
  }

  .discount-banner > span {
    display: flex;
    flex-direction: column;
    text-align: center;
  }

  .discount-banner span {
    font-size: 14px;
  }

  .discount-banner .button {
    font-size: 12px;
    padding-top: 0;
    padding-bottom: 0;
  }
}

</style>
{{ product.metafields.shogun.optimizer }}
